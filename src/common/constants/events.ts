export enum UserEventType {
  Create = 'user.create',
  Update = 'user.update',
  Delete = 'user.delete',
  FindById = 'user.findById',
}

export enum AuthEventType {
  Register = 'auth.register',
  Login = 'auth.login',
}
